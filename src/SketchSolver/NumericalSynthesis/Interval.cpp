#include "Interval.h"

Interval Interval::interval_add(Interval a, Interval b) {

}

Interval Interval::interval_sub(Interval a, Interval b) {
	
}

Interval Interval::interval_mul(Interval a, Interval b) {
	
}

Interval Interval::interval_div(Interval a, Interval b) {
	
}

Interval Interval::interval_neg(Interval a) {
	
}

Interval Interval::interval_mux(Interval a, Interval b){
	
}

bool Interval::interval_equal(Interval a, Interval b) {
	
}

bool Interval::interval_lt(Interval a, Interval b) {
	
}
